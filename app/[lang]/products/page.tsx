import {Locale} from "@/i18n.config";

export default function Products({params: {lang}}: { params: { lang: Locale } }) {
    return (
        <div>
            <h2>Products</h2>
        </div>
    );
}
